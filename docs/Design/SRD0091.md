# SQL Server Rule: SRD0091
  
|    |    |
|----|----|
| Assembly | SqlServer.Rules |
| Namespace | SqlServer.Rules.Design |
| Class | AvoidOrderByInDerivedTableRule |
  
## Rule Information
  
|    |    |
|----|----|
| Id | SRD0091 |
| Friendly Name | Avoid ORDER BY in derived tables for final ordering |
| Category | Design |
| Ignorable | false |
| Applicable Types | Procedure  |
|   | Scalar Function |
|   | Table Valued Function |
|   | View |
  
## Description
  
Ordering in a derived table does not guarantee result set ordering.
  
## Summary
  
Ordering in a derived table does not guarantee result set ordering.
  
### Examples
  
```sql
CREATE PROCEDURE [dbo].[OrderByInDerivedTableTest]
AS
BEGIN
    SET NOCOUNT ON;

    SELECT [dt].[Col1]
    FROM
    (
        SELECT TOP (10) [Col1]
        FROM [dbo].[TestTableSSDT]
        ORDER BY [Col1]
    ) AS [dt];
END;
RETURN 0;

-- SRD0091
```
  
### Remarks
  
Ordering rows inside a derived table does not guarantee the final outer query result order.
Apply ORDER BY at the outermost query level when deterministic final ordering is required.
  
<sub><sup>Generated by a tool</sup></sub>
